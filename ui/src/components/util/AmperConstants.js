export default class AmperConstatns {
    static SYSTEM_FIELDS = {
        ID: 'id',
        NAME: 'name',
        STATUS: 'status',
    };

    static INTEGER = {
        MAX_VALUE: Number.MAX_SAFE_INTEGER,
    }
}